diff --git a/src/main/java/it/ajneb97/libs/actionbarapi/ActionBarAPI.java b/src/main/java/it/ajneb97/libs/actionbarapi/ActionBarAPI.java
index 00cf974..2c1fd01 100644
--- a/src/main/java/it/ajneb97/libs/actionbarapi/ActionBarAPI.java
+++ b/src/main/java/it/ajneb97/libs/actionbarapi/ActionBarAPI.java
@@ -86,20 +86,18 @@ public class ActionBarAPI {
    public static void sendActionBar(final Player var0, final String var1, int var2, Interactions var3) {
       sendActionBar(var0, var1);
       if (var2 >= 0) {
-         (new BukkitRunnable() {
-            public void run() {
-               ActionBarAPI.sendActionBar(var0, "");
-            }
-         }).runTaskLater(var3, var2 + 1);
+         // Luma start - Folia support
+         dev.lumas.interactions.FoliaUtils.runOnEntityLater(var3, var0, () ->
+                 ActionBarAPI.sendActionBar(var0, ""), var2 + 1);
+         // Luma end - Folia support
       }
 
       while (var2 > 40) {
          var2 -= 40;
-         (new BukkitRunnable() {
-            public void run() {
-               ActionBarAPI.sendActionBar(var0, var1);
-            }
-         }).runTaskLater(var3, var2);
+         // Luma start - Folia support
+         dev.lumas.interactions.FoliaUtils.runOnEntityLater(var3, var0, () ->
+                 ActionBarAPI.sendActionBar(var0, var1), var2);
+         // Luma end - Folia support
       }
    }
 }
